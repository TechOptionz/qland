# Optional. When set, the site assistant (/api/chat) answers with Claude.
# Without it the widget still works and returns the "call us" fallback reply.
# Add this in Vercel under Project -> Settings -> Environment Variables.
ANTHROPIC_API_KEY=
